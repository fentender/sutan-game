name: CI macOS

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: macos-14
    strategy:
      matrix:
        python-version: ["3.14"]
    env:
      CMAKE_OSX_ARCHITECTURES: "arm64;x86_64"

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: pip install . -v

      - name: Verify sultan_core import
        run: python -c "import sultan_core; print(f'sultan_core {sultan_core.__version__}')"

      - name: C++ unit tests
        run: |
          pip install nanobind
          nb_dir=$(python -c "import nanobind; print(nanobind.cmake_dir())")
          cmake -B build_test -DBUILD_TESTS=ON -Dnanobind_DIR="$nb_dir"
          cmake --build build_test --target sultan_tests --config Release
          ctest --test-dir build_test -C Release --output-on-failure

      - name: Python tests
        run: python -m tests --func

  build:
    needs: test
    runs-on: macos-14
    env:
      CMAKE_OSX_ARCHITECTURES: "arm64;x86_64"
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install package
        run: pip install . -v

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Build history config pack
        run: python packaging/build_history_pack.py

      - name: Extract version
        id: version
        run: |
          ver=$(python -c "import re; print(re.search(r'APP_VERSION\s*=\s*\"([^\"]+)\"', open('src/config.py', encoding='utf-8').read()).group(1))")
          echo "version=$ver" >> "$GITHUB_OUTPUT"

      - name: Generate macOS icon
        run: |
          mkdir -p app.iconset
          sips -s format png app.ico --out app_icon.png 2>/dev/null || cp app.ico app_icon.png
          for size in 16 32 64 128 256 512; do
            sips -z $size $size app_icon.png --out "app.iconset/icon_${size}x${size}.png"
            double=$((size * 2))
            if [ $double -le 1024 ]; then
              sips -z $double $double app_icon.png --out "app.iconset/icon_${size}x${size}@2x.png"
            fi
          done
          iconutil -c icns app.iconset -o app.icns

      - name: PyInstaller build
        run: pyinstaller sudan_macos.spec --noconfirm

      - name: Create macOS archive
        run: |
          archive="SuDanModMerger-macOS-V${{ steps.version.outputs.version }}.zip"
          (
            cd dist
            ditto -c -k --sequesterRsrc --keepParent SuDanModMerger.app "../$archive"
          )

      - name: Verify packaged application
        run: |
          app="dist/SuDanModMerger.app"
          archive="SuDanModMerger-macOS-V${{ steps.version.outputs.version }}.zip"
          check_dir="$(mktemp -d)"
          trap 'rm -rf "$check_dir"' EXIT

          original_symlinks="$(find "$app" -type l | wc -l | tr -d '[:space:]')"
          if [ "$original_symlinks" -eq 0 ]; then
            echo "The PyInstaller bundle contains no symbolic links" >&2
            exit 1
          fi

          echo "Verifying the source application bundle"
          if ! codesign --verify --deep --strict --verbose=4 "$app"; then
            echo "Symbolic links in the source application:" >&2
            find "$app" -type l -exec ls -ld {} \; >&2
            exit 1
          fi

          ditto -x -k "$archive" "$check_dir"
          packaged_app="$check_dir/SuDanModMerger.app"
          if [ ! -d "$packaged_app" ]; then
            echo "Archive does not contain SuDanModMerger.app at its root" >&2
            find "$check_dir" -maxdepth 3 -print >&2
            exit 1
          fi
          packaged_symlinks="$(find "$packaged_app" -type l | wc -l | tr -d '[:space:]')"

          if [ "$packaged_symlinks" -ne "$original_symlinks" ]; then
            echo "Symbolic link count changed: $original_symlinks -> $packaged_symlinks" >&2
            exit 1
          fi

          test -f "$packaged_app/Contents/Resources/docs/用户使用手册.md"
          test "$(plutil -extract CFBundleShortVersionString raw -o - "$packaged_app/Contents/Info.plist")" = "${{ steps.version.outputs.version }}"
          test "$(plutil -extract CFBundleVersion raw -o - "$packaged_app/Contents/Info.plist")" = "${{ steps.version.outputs.version }}"
          test "$(plutil -extract LSMinimumSystemVersion raw -o - "$packaged_app/Contents/Info.plist")" = "15.0"

          echo "Verifying the archived application bundle"
          if ! codesign --verify --deep --strict --verbose=4 "$packaged_app"; then
            echo "Symbolic links in the archived application:" >&2
            find "$packaged_app" -type l -exec ls -ld {} \; >&2
            exit 1
          fi
          file "$packaged_app/Contents/MacOS/SuDanModMerger"
          echo "Verified $packaged_symlinks symbolic links in $archive"

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: macos-build
          path: SuDanModMerger-macOS-V*.zip

    outputs:
      version: ${{ steps.version.outputs.version }}

  release:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.build.outputs.version }}
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
    steps:
      - uses: actions/checkout@v6

      - name: Download artifact
        uses: actions/download-artifact@v5
        with:
          name: macos-build
          path: artifacts

      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${VERSION}"
          ZIP_NAME="SuDanModMerger-macOS-V${VERSION}.zip"

          # 创建/更新 tag
          git tag -f "$TAG"
          git push origin "$TAG" --force

          # 检查 release 是否存在
          if gh release view "$TAG" > /dev/null 2>&1; then
            echo "Release $TAG 已存在，替换 macOS 附件"
            gh release delete-asset "$TAG" "$ZIP_NAME" --yes 2>/dev/null || true
            gh release upload "$TAG" "artifacts/$ZIP_NAME"
          else
            echo "创建新 Release $TAG"
            gh release create "$TAG" \
              --title "[v${VERSION}] Mod 冲突合并工具" \
              --generate-notes \
              "artifacts/$ZIP_NAME"
          fi

      - name: Sync Gitee release
        if: env.GITEE_TOKEN != ''
        run: |
          TAG="v${VERSION}"
          ZIP_NAME="SuDanModMerger-macOS-V${VERSION}.zip"
          TITLE="[v${VERSION}] Mod 冲突合并工具"
          API="https://gitee.com/api/v5/repos/fentende125/sutan-game/releases"

          CURL_OPTS="--connect-timeout 30 --retry 3 --retry-delay 5"

          RELEASE_ID=$(curl -s $CURL_OPTS "${API}?access_token=${GITEE_TOKEN}" | \
            python3 -c "import sys,json; releases=json.load(sys.stdin); print(next((r['id'] for r in releases if r.get('tag_name')=='${TAG}'), ''))" 2>/dev/null || true)

          if [ -z "$RELEASE_ID" ]; then
            RESPONSE=$(curl -s $CURL_OPTS -X POST "$API" \
              -H "Content-Type: application/json" \
              -d "{\"access_token\":\"${GITEE_TOKEN}\",\"tag_name\":\"${TAG}\",\"name\":\"${TITLE}\",\"body\":\"CI 自动发布 v${VERSION}\",\"target_commitish\":\"master\"}")
            RELEASE_ID=$(echo "$RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin).get('id',''))" 2>/dev/null || true)
          fi

          if [ -n "$RELEASE_ID" ]; then
            curl -s $CURL_OPTS -X POST "${API}/${RELEASE_ID}/attach_files" \
              -F "access_token=${GITEE_TOKEN}" \
              -F "file=@artifacts/${ZIP_NAME}" || echo "Gitee 附件上传失败"
          fi
